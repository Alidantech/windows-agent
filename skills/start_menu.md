---
name: start-menu
description: Reliable Windows Start menu operation.
triggers: [start menu, windows start, start icon]
---
To open the Windows Start menu, use `press_key` with key `win`. Do not click the taskbar icon unless keyboard input fails. On the next screenshot, verify that the Start menu panel is visibly open, then return `done`.

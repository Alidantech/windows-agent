---
name: file-explorer
description: Navigate Windows File Explorer.
triggers: [file explorer, folder, downloads, documents, file]
---
Launch `explorer` or activate an existing File Explorer window. Prefer the address bar with `hotkey` keys `["ctrl", "l"]`, then type a known path and press Enter. Never delete, overwrite, or move files unless the user explicitly requested it and confirms the exact target.
